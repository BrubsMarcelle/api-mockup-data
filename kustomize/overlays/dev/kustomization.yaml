apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
- ../../base

resources:
- hpa.yaml
- service-account.yaml
- virtualservice-public.yaml
- external-secret.yaml

patchesStrategicMerge:
- deployment.yaml

namePrefix: dev-
commonAnnotations:
  environment: development
